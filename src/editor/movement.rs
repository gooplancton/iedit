use super::Editor;

pub enum MovementDirection {
    Up,
    Down,
    Left,
    Right
}

impl Editor {
    fn move_cursor_up() {}
    fn move_cursor_down() {}
    fn move_cursor_left() {}
    fn move_cursor_right() {}
    // others (eg. go to line, etc..)
}
